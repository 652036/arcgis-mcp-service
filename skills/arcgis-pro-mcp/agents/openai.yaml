interface:
  display_name: "ArcGIS Pro MCP"
  short_description: "Control ArcGIS Pro files and live windows safely"
  default_prompt: "Use $arcgis-pro-mcp to inspect or safely control ArcGIS Pro, explicitly choosing file mode, the Python CURRENT host, or the native SDK bridge."

dependencies:
  tools:
    - type: "mcp"
      value: "arcgis-pro"
      description: "ArcGIS Pro MCP stdio server for constrained ArcPy files, Python CURRENT routing, and the authenticated native SDK bridge"
      transport: "stdio"
